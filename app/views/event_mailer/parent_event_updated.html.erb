<h2><%=@junior.first_name.titleize%>'s <%= @event.game_type_string.titleize %> has changed</h2>

<p><%= @parent.first_name.titleize %>, details about <%= @junior.first_name.titleize %>'s <%= @event.game_type_string(false) %> have changed. Respond with your availabily now!</p>

<%= render :partial => "user_mailer/event_details", :locals => {:event => @event, :time_zone_mismatch => false} %>

<h2>Changes made:</h2>

<div class="event-update">
  <% @updates.each do |key, value| %>
    <p><strong><%= key.to_s %></strong> updated to '<%= value %>'</p>
  <% end %> 
</div>

<a class="button small" href="<%= event_url(@event) %>">View <%= @event.game_type_string %> Details</a>

<%
email_title = "Event+Details+Updated"
extra_params = @parent.email + "&EventId=" + @event.id.to_s  + "&relation=parent"
%>
<%== get_img_html(email_title, extra_params) %>